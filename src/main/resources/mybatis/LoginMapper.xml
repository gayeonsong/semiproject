<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.store.mapper.LoginMapper">
    <insert id="LoginInsert" parameterType="LoginDto">
        <selectKey keyProperty="user_id" resultType="int" order="BEFORE">
            select seq_login.nextval from dual
        </selectKey>
        insert into login values(#{user_id},#{login_id},#{password},#{role})
    </insert>
    <select id="countById" parameterType="string" resultType="int">
        select count(*) from login where login_id=#{login_id}
    </select>
    <update id="LoginUpdate" parameterType="StoreUpdateDto">
        update login
        set password = #{password, jdbcType=VARCHAR}
        where user_id = #{user_id, jdbcType=INTEGER}
    </update>

</mapper>